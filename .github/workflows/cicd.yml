name: CI/CD

on:
  pull_request:

jobs:
  backend-unit-tests:
    name: Backend Unit Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    env:
      DATABASE_URL: "postgresql://postgres:postgres@localhost:5433/test_fire_db"
      JWT_SECRET_KEY: your-super-secret-key-change-this
      MINIO_ACCESS_KEY: admin
      MINIO_SECRET_KEY: miniosupersecretpassword
      MINIO_BUCKET: fire-reports
      MINIO_ENDPOINT: localhost:9000
      SEED_DEFAULT_PASSWORD: ${{ secrets.SEED_DEFAULT_PASSWORD }}
      SEED_ADMIN_PASSWORD: ${{ secrets.SEED_ADMIN_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: ./.github/actions/setup-backend
      
      - name: Run unit tests
        run: |
          coverage erase
          pytest -c app/backend/testing/pytest.ini \
                 --cov=app/backend \
                 --cov-config=app/backend/testing/.coveragerc \
                 --cov-report=xml:app/backend/testing/coverage.xml \
                 app/backend/testing/unit
      
      - name: Post coverage comment
        uses: py-cov-action/python-coverage-comment-action@v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COVERAGE_PATH: app/backend/testing
          SUBPROJECT_ID: unit

  backend-integration-tests:
    name: Backend Integration Tests
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    services:
      postgres:
        image: postgis/postgis@sha256:a2fc46b52819d25add4ed93d3da83e106e17ea3ace6883cce3791631c9820bb2
        env: 
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_fire_db
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d test_fire_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      minio:
          image: minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
          env:
            MINIO_ROOT_USER: admin
            MINIO_ROOT_PASSWORD: miniosupersecretpassword
          ports:
            - 9000:9000
          command: server /data
          options: >-
            --health-cmd "curl -f http://localhost:9000/minio/health/ready || exit 1"
            --health-interval 10s
            --health-timeout 5s
            --health-retries 5
      valkey:
        image: valkey/valkey@sha256:3acc0687f2a2e1091fae6450d7842dd658c941338cf0a873ddd9e14b9e4ea4dd
        ports:
          - 6379:6379
        options: >-
          --health-cmd "valkey-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: "postgresql://postgres:postgres@localhost:5433/test_fire_db"
      JWT_SECRET_KEY: your-super-secret-key-change-this
      MINIO_ENDPOINT: localhost:9000
      MINIO_ACCESS_KEY: admin
      MINIO_SECRET_KEY: miniosupersecretpassword
      MINIO_SECURE: "false"
      MINIO_BUCKET: fire-reports
      VALKEY_HOST: localhost
      VALKEY_PORT: 6379
      SEED_DEFAULT_PASSWORD: ${{ secrets.SEED_DEFAULT_PASSWORD }}
      SEED_ADMIN_PASSWORD: ${{ secrets.SEED_ADMIN_PASSWORD }}
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: ./.github/actions/setup-backend

      - name: Run integration tests
        run: |
          coverage erase
          pytest -c app/backend/testing/pytest.ini \
                 --cov=app/backend \
                 --cov-config=app/backend/testing/.coveragerc \
                 --cov-report=xml:app/backend/testing/coverage.xml \
                 app/backend/testing/integration

      - name: Post coverage comment
        uses: py-cov-action/python-coverage-comment-action@v3
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COVERAGE_PATH: app/backend/testing
          SUBPROJECT_ID: integration

          

  backend-pylint:
    name: Backend Lint (pylint)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: ./.github/actions/setup-backend

      - name: Run pylint
        run: PYTHONPATH=. pylint --rcfile=app/backend/.pylintrc --recursive=y app --fail-under=8.0

  frontend-build:
    name: Frontend Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app/frontend
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: app/frontend/yarn.lock

      - name: Install dependencies
        run: yarn install

      - name: Build
        run: yarn run build

  frontend-eslint:
    name: Frontend Lint (ESLint)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app/frontend
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: app/frontend/yarn.lock

      - name: Install dependencies
        run: yarn install

      - name: Run ESLint
        run: yarn eslint . --ext .js,.jsx,.ts,.tsx

  frontend-react-doctor:
    name: React Doctor
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      statuses: write

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          fetch-depth: 0

      - name: Run React Doctor
        uses: millionco/react-doctor@736abc183ac491b4e954fc6bedec3a9a1b73d38b
        with:
          directory: app/frontend
          blocking: error

  frontend-e2e:
    name: Frontend E2E (Playwright)
    runs-on: ubuntu-latest
    needs: frontend-build
    services:
      postgres:
        image: postgis/postgis@sha256:a2fc46b52819d25add4ed93d3da83e106e17ea3ace6883cce3791631c9820bb2
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_fire_db
        ports:
          - 5433:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d test_fire_db"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      minio:
        image: minio/minio@sha256:14cea493d9a34af32f524e538b8346cf79f3321eff8e708c1e2960462bd8936e
        env:
          MINIO_ROOT_USER: admin
          MINIO_ROOT_PASSWORD: miniosupersecretpassword
        ports:
          - 9000:9000
        command: server /data
      valkey:
        image: valkey/valkey@sha256:3acc0687f2a2e1091fae6450d7842dd658c941338cf0a873ddd9e14b9e4ea4dd
        ports:
          - 6379:6379
        options: >-
          --health-cmd "valkey-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: ./.github/actions/setup-backend
        with:
          dev: "false"

      - name: Launch backend
        env:
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5433/test_fire_db"
          MINIO_ENDPOINT: localhost:9000
          MINIO_ACCESS_KEY: admin
          MINIO_SECRET_KEY: miniosupersecretpassword
          MINIO_BUCKET: fire-reports
          JWT_SECRET_KEY: ${{ secrets.JWT_SECRET_KEY || 'ci-test-key'}}
          VALKEY_HOST: localhost
          VALKEY_PORT: 6379
        run: |
          PYTHONPATH=. uvicorn app.backend.main:app --host 0.0.0.0 --port 8000 &
          for i in {1..30}; do
            curl -sf http://localhost:8000/health > /dev/null && break
            sleep 1
          done

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: app/frontend/yarn.lock

      - name: Install frontend dependencies
        working-directory: app/frontend
        run: yarn install

      - name: Install Playwright browsers
        working-directory: app/frontend
        run: yarn playwright install --with-deps

      - name: Run Playwright Tests
        working-directory: app/frontend
        env:
          CI: true
          BACKEND_INTERNAL_URL: "http://localhost:8000"
        run: yarn test

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: app/frontend/playwright-report/
          retention-days: 7

 